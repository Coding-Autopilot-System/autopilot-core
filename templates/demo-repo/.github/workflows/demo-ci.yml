name: Demo CI

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  demo:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Intentional failure
        run: |
          echo "Simulated failure for Autopilot demo."
          exit 1
